- model_class = Batch
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize.titleize

= react_component 'Batches', { batches: @batches,
                               units: UnitPresenter.collection(Unit.all).as_json }
